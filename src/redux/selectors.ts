export const selectAddress = (state: any) => state.wallet.address;

export const selectBalance = (state: any) => state.wallet.balance;

export const selecSigner = (state: any) => state.wallet.signer;
